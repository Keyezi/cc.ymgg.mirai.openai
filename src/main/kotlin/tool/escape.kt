package cc.ymgg.openai.tool

import net.mamoe.mirai.message.data.MessageChain

object escape {
    fun WechatToMirai(text:String): MessageChain.Companion {
        
        return MessageChain
    }
    fun MiraiToWechat(msgchain: MessageChain):String{
        for (i in msgchain) {
        
        }
        return ""
    }
}